let userAge = 17;

let massage =
  userAge >= 18 ? "Eligible for booking" : "Not Eligible for booking";

console.log(massage);

let isEligible = userAge >= 18 ? true : false;

if (isEligible) {
  console.log("Eligible for booking");
} else {
  console.log("Not Eligible for booking");
}
