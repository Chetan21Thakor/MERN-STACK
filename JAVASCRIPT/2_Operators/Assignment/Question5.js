// ! 5. Check if a number is within a range between 10 and 20 (inclusive).

let num=12;

let res = (num >= 10 && num<=20) ? true : false;

if(res){
    console.log(`${num} is Between 10 to 20.`);
}else{
    console.log(`${num} is not  Between 10 to 20.`);
}